#!/usr/bin/pythons3

def magic_calculation(a, b):
    """Matches bytecode that is provided"""
    from magic_calculation_102 import, sub

    if a < b:
        c = add(a, b)
        for i in range(4, 6):
            c = add(c, i)
        return (c)

    else:
        return(sub(a, b))
